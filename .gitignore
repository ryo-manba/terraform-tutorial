# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log

# Ignore all .tfvars files, which are likely to contain sentitive data,
# except for the example tfvars file
*.tfvars
!example.tfvars

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore provider binary files
terraform-provider-*

# Exclude all .tfplan files, which are generated by terraform plan commands
*.tfplan
*.tfplan.*
